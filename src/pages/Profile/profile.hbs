{{!-- {{#extend "ProfileLayout"}}
  {{#content body}} --}}
    <div class="page_profile">
      {{{LinkBack}}}
      {{#if isLoading}}
        <div class="isLoading">
          Loading...
        </div>
      {{else}}
          {{{BodyProfile}}}
      {{/if}}
    </div>
  {{!-- {{/content}}
{{/extend}} --}}